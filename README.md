# HomeMate

## Setup

1. Copy `.env.example` to `.env.dev` (or `.env.dev.local`)
2. Fill Supabase URL and anon key
3. Run `npm install` and `npm run dev`

For the integration environment (int), copy `.env.example` to `.env.int` (or `.env.int.local`) and run `npm run int`.